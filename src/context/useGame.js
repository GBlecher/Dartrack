/*
 * useGame.js
 * Placeholder / compatibility file. The project exposes the real hook
 * via `GameContext.jsx` as `useGame`. This file is intentionally
 * left empty so imports that reference it don't cause runtime errors
 * if a different export strategy is used in the future.
 */

export {}; // keep module syntax valid for bundlers
